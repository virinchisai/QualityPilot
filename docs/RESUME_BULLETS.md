# Resume bullets

- Built a Python/FastAPI TestOps platform that ingests Markdown, YAML, JSON, plain text, Gherkin, and OpenAPI requirements and produces Pydantic-validated test cases with requirement-to-Gherkin traceability.
- Implemented a SQLite/SQLAlchemy enterprise demo application with salted PBKDF2 password storage, signed JWT access tokens, rotating refresh tokens, replay rejection, token-family logout revocation, protected profiles, role-based admin authorization, and login rate limiting.
- Created pytest/HTTPX-compatible API, identity, and security regression suites covering invalid payloads, token signature/expiry/type/revocation, refresh rotation, database changes, missing authentication, and RBAC restrictions.
- Developed Playwright TypeScript Page Objects and browser tests for login, logout, profile changes, protected navigation, session expiry, mobile layout, accessible locators, screenshots, video, traces, retries, axe checks, and opt-in visual/flaky demonstrations.
- Delivered deterministic failure classification, SQLite execution history, transition-based flaky-test scoring, evidence manifests, and defect serialization to Markdown, JSON, and Jira-compatible payloads.
- Built an offline AI-agent quality harness that checks prompt-injection refusal, grounding and citation validity, structured output, tool allow-lists, unsafe actions, human approval gates, and golden-answer regression without requiring a paid model API.
- Automated Python, Behave, Playwright, Docker, CodeQL, Dependabot, and opt-in OWASP ZAP workflows with health-gated services, JUnit/HTML output, failure evidence retention, artifact upload, and GitHub Actions summaries.
- Added deterministic JSON Schema event validation for duplicate IDs, per-aggregate ordering, invalid events, and dead-letter routing concepts, plus a working Locust load profile and Prometheus-compatible application metrics.

